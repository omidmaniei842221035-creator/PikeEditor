# راهنمای عیب‌یابی نسخه Windows

## 🔍 یافتن فایل لاگ

اگر برنامه با خطا مواجه شد، فایل لاگ در این آدرس ذخیره می‌شود:

```
C:\Users\[نام کاربری]\AppData\Roaming\سامانه مانیتورینگ POS\logs\
```

یا:

1. کلید **Windows + R** را بزنید
2. تایپ کنید: `%APPDATA%\سامانه مانیتورینگ POS\logs`
3. Enter بزنید

## 📝 محتوای فایل لاگ

فایل لاگ شامل اطلاعات زیر است:
- مسیرهای فایل‌ها و پوشه‌ها
- خطاهای سرور
- وضعیت دیتابیس
- پیام‌های debug

## ❌ مشکلات متداول

### 1. برنامه اصلاً اجرا نمی‌شود
**علت احتمالی:**
- فایل server به درستی package نشده
- دیتابیس SQLite نمی‌تواند ایجاد شود

**راه حل:**
1. فایل لاگ را باز کنید
2. به دنبال خطاهای "[SERVER ERROR]" بگردید
3. بررسی کنید آیا مسیر دیتابیس قابل نوشتن است

### 2. صفحه سفید نمایش داده می‌شود
**علت احتمالی:**
- سرور شروع نشده یا port 5000 اشغال است
- Frontend فایل‌ها به درستی load نمی‌شوند

**راه حل:**
1. منتظر بمانید 10 ثانیه (سرور زمان نیاز دارد)
2. برنامه را ببندید و دوباره باز کنید
3. فایل لاگ را چک کنید

### 3. خطای JavaScript
**علت احتمالی:**
- بستگی‌های native (better-sqlite3) به درستی rebuild نشده‌اند
- فایل‌های dist به درستی compile نشده‌اند

**راه حل:**
1. برنامه را کاملاً uninstall کنید
2. دوباره نصب کنید
3. مطمئن شوید که با این دستورات build شده:
   ```bash
   npm install
   npm rebuild better-sqlite3 --update-binary
   npm run build
   npm run electron:compile
   npm run electron:build:win
   ```

## 🗂️ محل فایل‌های برنامه

### نسخه Portable
همه چیز در یک پوشه کنار فایل .exe است

### نسخه Installer
- **فایل‌های برنامه:** `C:\Users\[نام]\AppData\Local\Programs\سامانه مانیتورینگ POS\`
- **دیتابیس:** `C:\Users\[نام]\AppData\Roaming\سامانه مانیتورینگ POS\pos-system.db`
- **لاگ‌ها:** `C:\Users\[نام]\AppData\Roaming\سامانه مانیتورینگ POS\logs\`

## 📧 گزارش مشکل

اگر مشکل برطرف نشد، فایل لاگ را ارسال کنید به همراه:
1. نسخه Windows (10 یا 11)
2. نسخه برنامه (از About > Version)
3. چگونگی بروز مشکل (قدم به قدم)
4. محتوای کامل آخرین فایل لاگ

## 🛠️ ابزارهای کمکی

### دیدن Console در Electron:
اگر برنامه باز شد ولی کار نمی‌کند:
1. کلیدهای **Ctrl + Shift + I** را فشار دهید
2. تب "Console" را باز کنید
3. خطاهای JavaScript را ببینید

### اجرای Manual:
برای debug دقیق‌تر:
```bash
cd "C:\Users\[نام]\AppData\Local\Programs\سامانه مانیتورینگ POS"
"سامانه مانیتورینگ POS.exe" --trace-warnings --inspect
```
