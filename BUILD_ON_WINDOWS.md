# 🖥️ راهنمای ساخت Installer در ویندوز

## مرحله 1: آماده‌سازی محیط ویندوز

### نصب پیش‌نیازها:

#### 1. نصب Node.js
- برو به: https://nodejs.org
- دانلود **LTS version** (توصیه می‌شود v20 یا بالاتر)
- نصب با تنظیمات پیش‌فرض
- تیک "Automatically install necessary tools" را بزن

#### 2. نصب Git
- برو به: https://git-scm.com/download/win
- دانلود و نصب با تنظیمات پیش‌فرض

#### 3. تست نصب
باز کن **Command Prompt** یا **PowerShell** و اجرا کن:
```bash
node --version
# باید خروجی بدهد: v20.x.x یا بالاتر

npm --version
# باید خروجی بدهد: 10.x.x یا بالاتر

git --version
# باید خروجی بدهد: git version 2.x.x
```

---

## مرحله 2: دانلود کد منبع

### روش 1: از Git Repository

```bash
# باز کردن PowerShell یا CMD
cd C:\Users\YourName\Desktop

# Clone کردن پروژه
git clone https://github.com/your-username/pos-monitoring.git

# وارد شدن به پوشه
cd pos-monitoring
```

### روش 2: دانلود ZIP از Replit

اگر Git ندارید:
1. در Replit، روی دکمه **Download as ZIP** کلیک کنید
2. فایل ZIP را استخراج کنید
3. پوشه را به دسکتاپ منتقل کنید

---

## مرحله 3: نصب Dependencies

باز کن **PowerShell** یا **Command Prompt** در پوشه پروژه:

```bash
# نصب تمام packages (ممکن است 2-5 دقیقه طول بکشد)
npm install
```

**نکته:** اگر خطا گرفتید، اجرا کنید:
```bash
npm install --legacy-peer-deps
```

---

## مرحله 4: ساخت Installer

### روش ساده (یک دستور):
```bash
npm run electron:build:win
```

این دستور:
1. ✅ Frontend را build می‌کند (Vite)
2. ✅ Backend را build می‌کند (esbuild)
3. ✅ Electron files را compile می‌کند
4. ✅ Windows installer را می‌سازد

### زمان تقریبی:
- اولین بار: **5-10 دقیقه** (دانلود Electron)
- دفعات بعد: **2-3 دقیقه**

### خروجی موفق:
```
✓ built in 2m 15s

Building target: nsis
• building        target=nsis
• built in 45s
```

---

## مرحله 5: یافتن فایل‌های ساخته شده

بعد از build موفق، فایل‌ها در پوشه `release/` هستند:

```
release/
├── سامانه مانیتورینگ POS-Setup-1.0.0.exe    ← فایل نصب اصلی (300-400MB)
└── win-unpacked/                              ← نسخه portable
    └── سامانه مانیتورینگ POS.exe             ← فایل اجرایی
```

---

## مرحله 6: تست Installer

### تست نصب:
1. فایل `سامانه مانیتورینگ POS-Setup-1.0.0.exe` را دوبار کلیک کنید
2. Wizard نصب باز می‌شود:
   - صفحه خوش‌آمدگویی
   - انتخاب مسیر نصب (پیش‌فرض: `C:\Program Files\`)
   - گزینه‌های میانبر (Desktop, Start Menu)
   - نوار پیشرفت نصب
   - اتمام نصب

3. بعد از نصب:
   - آیکون در Desktop ✅
   - آیکون در Start Menu ✅
   - برنامه در Control Panel > Programs ✅

### تست اجرا:
1. برنامه را از Desktop یا Start Menu باز کنید
2. باید بدون خطا اجرا شود و Dashboard باز شود
3. پایگاه داده SQLite خودکار ساخته می‌شود در:
   ```
   C:\Users\YourName\AppData\Roaming\pos-monitoring-system\pos-system.db
   ```

### تست حذف (Uninstall):
1. Control Panel > Programs and Features
2. پیدا کردن "سامانه مانیتورینگ POS"
3. کلیک راست > Uninstall
4. باید بدون خطا حذف شود

---

## ❗ رفع مشکلات رایج

### خطا: "npm is not recognized"
**حل:** Node.js درست نصب نشده. دوباره نصب کنید و کامپیوتر را restart کنید.

### خطا: "gyp ERR! stack Error: spawn python ENOENT"
**حل:** نیازی به کار خاصی نیست - این warning است نه error. build ادامه پیدا می‌کند.

### خطا: "EPERM: operation not permitted"
**حل:** 
1. Antivirus را موقتاً خاموش کنید
2. پوشه `node_modules` را پاک کنید
3. دوباره `npm install` کنید

### خطا: "Cannot find module 'electron'"
**حل:**
```bash
npm install electron --save-dev
npm run electron:build:win
```

### Build خیلی کند است
**نرمال است!** اولین build ممکن است 10 دقیقه طول بکشد چون:
- Electron (120MB) دانلود می‌شود
- Dependencies compile می‌شوند
- Frontend build می‌شود

---

## 📊 معیارهای موفقیت

بعد از build، باید این موارد را داشته باشید:

✅ فایل `Setup.exe` با حجم 300-400MB
✅ نصب بدون خطا
✅ اجرا بدون نیاز به Node.js
✅ پایگاه داده SQLite خودکار ساخته می‌شود
✅ حذف (Uninstall) بدون خطا

---

## 🎯 مراحل نهایی

### 1. تغییر نام فایل (اختیاری):
```bash
ren "سامانه مانیتورینگ POS-Setup-1.0.0.exe" "POS-Monitoring-Setup.exe"
```

### 2. ساخت نسخه portable هم:
```bash
npm run pack
```
خروجی: `release/win-unpacked/` - فقط این پوشه را zip کنید

### 3. آماده برای توزیع:
- فایل Setup.exe را به اشتراک بگذارید
- یا در وب‌سایت آپلود کنید
- یا در USB کپی کنید

---

## 💾 بک‌آپ فایل‌ها

**مهم:** این فایل‌ها را نگه دارید:
```
✅ release/سامانه مانیتورینگ POS-Setup-1.0.0.exe
✅ release/win-unpacked/ (نسخه portable)
✅ package.json (تنظیمات build)
✅ electron/main.ts (کد اصلی)
```

---

## 📞 پشتیبانی

اگر مشکلی پیش آمد:
1. پیغام خطا را کامل کپی کنید
2. فایل `npm-debug.log` را بررسی کنید
3. با تیم پشتیبانی تماس بگیرید

**موفق باشید!** 🎉
