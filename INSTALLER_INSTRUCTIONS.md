# راهنمای ساخت Installer ویندوزی

## ⚠️ وضعیت فعلی

**خبر بد:** در محیط Replit (Linux بدون Wine) امکان ساخت NSIS Installer وجود ندارد.

**خبر خوب:** تمام کدها آماده هستند و فقط باید روی یک کامپیوتر ویندوز build شوند!

---

## ✅ آماده برای Build در Windows

### فایل‌های آماده:
- ✅ `electron/main.ts` - اصلاح شده برای استفاده از Electron embedded runtime
- ✅ `package.json` - تنظیمات کامل NSIS installer  
- ✅ `build/icon.ico` - آیکون ویندوز
- ✅ `dist/` - Frontend & Backend ساخته شده
- ✅ `dist-electron/` - Electron files compiled

### تنظیمات NSIS در package.json:
```json
{
  "build": {
    "win": {
      "target": ["nsis"],
      "icon": "build/icon.ico"
    },
    "compression": "store",
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "سامانه مانیتورینگ POS"
    }
  }
}
```

---

## 🖥️ نحوه ساخت Installer در Windows

### پیش‌نیازها:
1. ویندوز 10 یا 11
2. Node.js v20+ (دانلود از nodejs.org)
3. Git (دانلود از git-scm.com)

### مراحل:

```bash
# 1. Clone کردن repository
git clone <your-repo-url>
cd pos-monitoring-system

# 2. نصب dependencies
npm install

# 3. Build کردن برنامه + Installer
npm run electron:build:win
```

### خروجی:
```
release/
├── سامانه مانیتورینگ POS-Setup-1.0.0.exe  ← فایل نصب (حدود 300MB)
└── win-unpacked/                          ← نسخه portable
```

---

## 🎯 فایل Setup.exe چه قابلیت‌هایی دارد؟

✅ **Wizard نصب کامل**
- صفحه خوش‌آمدگویی
- انتخاب مسیر نصب
- نوار پیشرفت
- میانبرهای خودکار

✅ **یکپارچه سازی با Windows**
- ثبت در Programs and Features
- آیکون در Start Menu
- میانبر در Desktop
- Uninstaller کامل

✅ **بدون نیاز به نصب‌های اضافی**
- همه وابستگی‌ها bundle شده
- SQLite داخلی
- هیچ Node.js یا چیز دیگری لازم نیست

---

## 📦 نسخه Portable (بدون نیاز به ویندوز)

اگر نمی‌خواهید منتظر Build در ویندوز بمانید، می‌توانید از نسخه portable استفاده کنید که **همین الان در Replit آماده است**:

### چطور استفاده کنم؟

**روش 1: Build کردن portable در Replit:**
```bash
npm run pack
```

خروجی: `release/win-unpacked/` (حدود 300MB)

**روش 2: فشرده کردن برای انتقال:**
```bash
cd release
tar -czf pos-monitoring-portable.tar.gz win-unpacked/
```

### مزایای Portable:
- ✅ بدون نیاز به نصب
- ✅ قابل حمل (USB stick)
- ✅ هیچ تغییری در رجیستری نمی‌دهد
- ✅ فقط پوشه را پاک کنید = حذف کامل

### نحوه اجرا:
1. استخراج فایل tar.gz
2. داخل پوشه `win-unpacked` بروید
3. فایل `electron.exe` را دوبار کلیک کنید

---

## 🔧 اصلاحات انجام شده

### مشکل قبلی:
```typescript
// ❌ نیاز به Node.js خارجی (روی ویندوز نبود)
serverProcess = spawn('node', [serverPath], { ... });
```

### حل شده:
```typescript
// ✅ استفاده از Electron embedded runtime
serverProcess = spawn(process.execPath, [serverPath], {
  env: {
    ...process.env,
    ELECTRON_RUN_AS_NODE: '1'  // کلید حل مشکل!
  }
});
```

این یعنی برنامه portable الان **کاملاً مستقل** است و هیچ نیازی به Node.js ندارد!

---

## 📊 مقایسه

| ویژگی | Portable (الان) | Setup.exe (Windows) |
|-------|----------------|---------------------|
| محیط ساخت | ✅ Replit | ❌ نیاز به Windows |
| نیاز به نصب | ❌ | ✅ |
| یکپارچگی با Windows | ⚠️ محدود | ✅ کامل |
| سادگی استفاده | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| سرعت آماده‌سازی | ⚡ فوری | 🐌 باید build شود |

---

## 💡 توصیه نهایی

### برای استفاده فوری:
→ از **نسخه Portable** استفاده کنید (همین الان آماده است!)

### برای توزیع حرفه‌ای:
→ روی یک کامپیوتر **Windows** installer بسازید

---

## 📞 پشتیبانی

اگر سؤالی دارید:
- 📧 info@posmonitoring.ir  
- 📱 [شماره تماس]
- 🌐 [وب‌سایت]

**موفق باشید!** 🎉
