# راهنمای ساخت نسخه Windows - سامانه مانیتورینگ POS

## نسخه 1.0.2 (نهایی)

---

## 📋 پیش‌نیازها

### 1. Node.js (ضروری)
- دانلود از: https://nodejs.org/en/download/
- نسخه LTS (20.x) را نصب کنید

### 2. Visual Studio Build Tools (ضروری)
- دانلود از: https://visualstudio.microsoft.com/visual-cpp-build-tools/
- در هنگام نصب **"Desktop development with C++"** را انتخاب کنید
- این برای کامپایل `better-sqlite3` ضروری است

### 3. Git (اختیاری ولی پیشنهادی)
- دانلود از: https://git-scm.com/download/win

---

## 🚀 مراحل Build

### مرحله 1: دانلود پروژه
فایل‌ها را از Replit به صورت ZIP دانلود کنید و در یک پوشه استخراج کنید.

### مرحله 2: اجرای Build
1. پوشه پروژه را باز کنید
2. فایل `BUILD_FINAL_v1.0.2.bat` را دوبار کلیک کنید
3. صبر کنید تا build کامل شود (10-15 دقیقه)

### مرحله 3: نصب برنامه
1. به پوشه `release` بروید
2. فایل `سامانه مانیتورینگ POS-Setup-1.0.2.exe` را پیدا کنید
3. با **Run as administrator** نصب کنید

---

## ⚠️ مشکلات رایج و راه‌حل‌ها

### مشکل: npm نمی‌تواند package‌ها را دانلود کند (در ایران)
**راه‌حل:**
1. فایل `FIX_NPM_IRAN.bat` را اجرا کنید
2. دوباره `BUILD_FINAL_v1.0.2.bat` را اجرا کنید

### مشکل: خطای `node-gyp` یا `better-sqlite3`
**راه‌حل:**
1. Visual Studio Build Tools را نصب کنید
2. گزینه "Desktop development with C++" را انتخاب کنید
3. کامپیوتر را ریستارت کنید

### مشکل: خطای `EACCES` یا دسترسی
**راه‌حل:**
- Command Prompt را با **Run as administrator** اجرا کنید

---

## 📁 ساختار فایل‌های مهم

```
pos-monitoring-system/
├── BUILD_FINAL_v1.0.2.bat     # اسکریپت اصلی build
├── FIX_NPM_IRAN.bat           # تنظیم npm برای ایران
├── electron/                   # فایل‌های Electron
│   ├── main.ts                # فایل اصلی Electron
│   ├── preload.ts             # Preload script
│   └── logger.ts              # سیستم لاگ
├── server/                     # سرور Express
├── client/                     # فرانت‌اند React
├── electron-package.json       # package.json برای Electron (بدون ESM)
├── server-package.json         # package.json برای سرور (با ESM)
└── package.json                # package.json اصلی
```

---

## 🔧 معماری نرم‌افزار

### دو حالت اجرا:

| ویژگی | نسخه وب | نسخه دسکتاپ |
|-------|---------|-------------|
| دیتابیس | PostgreSQL | SQLite |
| اجرا | در مرورگر | Electron |
| نصب | بدون نیاز | Installer |
| آفلاین | خیر | بله |

### چگونه کار می‌کند:
1. **Electron** یک پنجره دسکتاپ ایجاد می‌کند
2. **سرور Express** در پس‌زمینه اجرا می‌شود
3. **SQLite** داده‌ها را ذخیره می‌کند
4. فرانت‌اند React در پنجره نمایش داده می‌شود

---

## 📊 مشخصات خروجی

- **فایل نهایی**: `سامانه مانیتورینگ POS-Setup-1.0.2.exe`
- **اندازه تقریبی**: 150-200 MB
- **سیستم‌عامل**: Windows 10/11 (64-bit)
- **زبان رابط**: فارسی (راست به چپ)

---

## 📝 تغییرات نسخه 1.0.2

### رفع شده:
- ✅ خطای `exports is not defined in ES module scope`
- ✅ مشکل CommonJS/ESM در Electron
- ✅ مسیر سرور در حالت production

### اضافه شده:
- ✅ سیستم لاگ کامل
- ✅ دیالوگ خطا به فارسی
- ✅ package.json جداگانه برای Electron و سرور

---

## 💡 نکات مهم

1. **همیشه با Run as administrator نصب کنید**
2. **اگر نسخه قبلی نصب است، اول آن را Uninstall کنید**
3. **لاگ‌ها در این مسیر ذخیره می‌شوند:**
   `%APPDATA%\سامانه مانیتورینگ POS\logs\`

---

## 📞 پشتیبانی

در صورت بروز مشکل، فایل لاگ را بررسی کنید:
1. دکمه Win + R را بزنید
2. تایپ کنید: `%APPDATA%\سامانه مانیتورینگ POS\logs`
3. فایل لاگ را باز کنید و خطا را پیدا کنید
