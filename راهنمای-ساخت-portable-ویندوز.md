# 🔧 راهنمای کامل ساخت نسخه Portable ویندوز

## 📋 فهرست مطالب
1. [پیش‌نیازها](#پیش-نیازها)
2. [آماده‌سازی محیط](#آماده-سازی-محیط)
3. [دانلود پروژه](#دانلود-پروژه)
4. [نصب Dependencies](#نصب-dependencies)
5. [Build کردن Portable](#build-کردن-portable)
6. [تست نسخه Portable](#تست-نسخه-portable)
7. [رفع مشکلات](#رفع-مشکلات)

---

## 🎯 پیش‌نیازها

### 1. کامپیوتر ویندوز
- ✅ Windows 10 (64-bit) یا بالاتر
- ✅ حداقل 4GB RAM
- ✅ حداقل 5GB فضای خالی
- ✅ اتصال اینترنت (برای دانلود)

### 2. نرم‌افزارهای مورد نیاز
- Node.js (نسخه 20 یا بالاتر)
- Git (اختیاری - برای clone کردن)
- 7-Zip یا WinRAR (برای استخراج فایل‌ها)

---

## 📦 مرحله 1: نصب Node.js

### 1.1 دانلود Node.js

1. **باز کردن مرورگر:**
   - مرورگر را باز کنید (Chrome, Edge, Firefox)

2. **رفتن به سایت Node.js:**
   ```
   https://nodejs.org
   ```

3. **دانلود نسخه LTS:**
   - روی دکمه سبز رنگ **"LTS"** کلیک کنید
   - فایل `node-v20.x.x-x64.msi` دانلود می‌شود (حدود 30MB)

### 1.2 نصب Node.js

1. **اجرای فایل نصب:**
   - دوبار کلیک روی فایل دانلود شده
   - اگر پنجره UAC باز شد، روی **Yes** کلیک کنید

2. **صفحه Welcome:**
   - روی **Next** کلیک کنید

3. **صفحه License Agreement:**
   - تیک **"I accept..."** را بزنید
   - روی **Next** کلیک کنید

4. **صفحه Destination Folder:**
   - مسیر پیش‌فرض را نگه دارید: `C:\Program Files\nodejs\`
   - روی **Next** کلیک کنید

5. **صفحه Custom Setup:**
   - همه گزینه‌ها را روی پیش‌فرض بگذارید
   - **مهم:** تیک **"Add to PATH"** باید فعال باشد ✅
   - روی **Next** کلیک کنید

6. **صفحه Tools for Native Modules:**
   - تیک **"Automatically install necessary tools"** را بزنید ✅
   - روی **Next** کلیک کنید

7. **صفحه Ready to Install:**
   - روی **Install** کلیک کنید
   - صبر کنید (1-2 دقیقه)

8. **پنجره Command Prompt:**
   - یک پنجره سیاه باز می‌شود (نصب ابزارهای اضافی)
   - دکمه‌های زیادی ظاهر می‌شود - فقط **Enter** بزنید
   - صبر کنید (5-10 دقیقه) - ممکن است چند پنجره باز شود
   - وقتی تمام شد، پنجره بسته می‌شود

9. **اتمام نصب:**
   - روی **Finish** کلیک کنید

### 1.3 تست نصب Node.js

1. **باز کردن PowerShell:**
   - کلید **Windows** را فشار دهید
   - تایپ کنید: `powershell`
   - Enter بزنید

2. **تست Node.js:**
   ```powershell
   node --version
   ```
   
   **خروجی مورد انتظار:**
   ```
   v20.11.0
   ```
   (یا هر نسخه 20.x.x دیگر)

3. **تست npm:**
   ```powershell
   npm --version
   ```
   
   **خروجی مورد انتظار:**
   ```
   10.2.4
   ```
   (یا هر نسخه 10.x.x دیگر)

4. **اگر خطا گرفتید:**
   - کامپیوتر را **Restart** کنید
   - دوباره PowerShell را باز کنید و تست کنید

---

## 📁 مرحله 2: دانلود پروژه

### روش 1: دانلود ZIP از Replit (ساده‌تر ✅)

#### 2.1.1 دانلود از Replit

1. **باز کردن Replit:**
   - مرورگر را باز کنید
   - به پروژه Replit بروید

2. **دانلود ZIP:**
   - روی آیکن **⋮** (سه نقطه) کنار نام پروژه کلیک کنید
   - گزینه **"Download as ZIP"** را انتخاب کنید
   - فایل `pos-monitoring-system.zip` دانلود می‌شود (حدود 300MB)

#### 2.1.2 استخراج ZIP

1. **ایجاد پوشه:**
   - رفتن به Desktop
   - کلیک راست → **New** → **Folder**
   - نام: `POS-Project`

2. **استخراج فایل:**
   - فایل `pos-monitoring-system.zip` را به پوشه `POS-Project` کپی کنید
   - کلیک راست روی فایل ZIP
   - **Extract Here** (با 7-Zip یا WinRAR)
   - صبر کنید (2-3 دقیقه)

3. **نتیجه:**
   ```
   Desktop/
   └── POS-Project/
       └── pos-monitoring-system/
           ├── client/
           ├── server/
           ├── electron/
           ├── package.json
           └── ...
   ```

### روش 2: Clone با Git (برای کسانی که Git دارند)

#### 2.2.1 نصب Git (اگر ندارید)

1. **دانلود Git:**
   ```
   https://git-scm.com/download/win
   ```

2. **نصب Git:**
   - دوبار کلیک روی فایل دانلود شده
   - همه گزینه‌ها را پیش‌فرض بگذارید
   - روی **Next** و **Install** کلیک کنید

#### 2.2.2 Clone پروژه

1. **باز کردن PowerShell:**
   - کلیک راست در Desktop
   - **Open in Terminal** یا **PowerShell**

2. **Clone کردن:**
   ```powershell
   git clone https://github.com/your-username/pos-monitoring.git
   cd pos-monitoring
   ```

---

## 📦 مرحله 3: نصب Dependencies

### 3.1 باز کردن پوشه پروژه

1. **باز کردن PowerShell در پوشه پروژه:**
   
   **روش 1 - از File Explorer:**
   - رفتن به پوشه پروژه: `Desktop\POS-Project\pos-monitoring-system`
   - کلیک روی نوار آدرس بالا (جایی که مسیر نوشته)
   - تایپ کنید: `powershell`
   - Enter بزنید
   
   **روش 2 - از PowerShell:**
   ```powershell
   cd Desktop
   cd POS-Project
   cd pos-monitoring-system
   ```

2. **تأیید مسیر:**
   ```powershell
   pwd
   ```
   
   **خروجی مورد انتظار:**
   ```
   C:\Users\YourName\Desktop\POS-Project\pos-monitoring-system
   ```

### 3.2 بررسی فایل package.json

```powershell
ls package.json
```

**خروجی مورد انتظار:**
```
package.json
```

اگر این فایل را ندیدید، یعنی در پوشه اشتباهی هستید!

### 3.3 نصب Dependencies

1. **اجرای دستور نصب:**
   ```powershell
   npm install
   ```

2. **منتظر بمانید:**
   - این مرحله **3-10 دقیقه** طول می‌کشد
   - خیلی چیزها دانلود و نصب می‌شود
   
   **خروجی‌هایی که می‌بینید:**
   ```
   npm WARN deprecated ...
   added 1234 packages in 5m
   ```

3. **نشانه‌های موفقیت:**
   - ✅ پوشه `node_modules` ساخته شده
   - ✅ فایل `package-lock.json` ساخته شده
   - ✅ هیچ خطای قرمز رنگی نیست

4. **تأیید نصب:**
   ```powershell
   ls node_modules
   ```
   
   باید لیست طولانی از پوشه‌ها ببینید (حدود 1000+ پوشه)

### 3.4 Rebuild Native Modules برای Windows

**این مرحله بسیار مهم است! ⚠️**

این دستور `better-sqlite3` را برای Windows compile می‌کند:

```powershell
npm rebuild better-sqlite3 --update-binary
```

**خروجی مورد انتظار:**
```
> better-sqlite3@9.x.x rebuild
> node-gyp rebuild

Building the projects in this solution one at a time...
...
better-sqlite3@9.x.x node_modules\better-sqlite3
```

اگر خطا گرفتید، ادامه دهید - مرحله بعد دوباره rebuild می‌کند.

---

## 🔨 مرحله 4: Build کردن Portable

### 4.1 Build کردن Frontend و Backend

```powershell
npm run build
```

**زمان:** 2-3 دقیقه

**خروجی مورد انتظار:**
```
> build
> concurrently "npm run build:frontend" "npm run build:backend"

[0] > build:frontend
[0] vite build
[1] > build:backend
[1] esbuild ...

[0] ✓ built in 45s
[1] Build complete
```

**فایل‌های ساخته شده:**
- ✅ پوشه `dist/` (Frontend)
- ✅ پوشه `dist-electron/` (Backend)

### 4.2 Build کردن Electron

```powershell
npm run electron:build
```

**زمان:** 1-2 دقیقه

**خروجی مورد انتظار:**
```
> electron:build
> tsc -p electron/tsconfig.json

(بدون خطا)
```

### 4.3 ساخت نسخه Portable

**حالا مرحله اصلی! 🎯**

```powershell
npm run electron:build:win -- --target portable
```

یا فقط برای installer معمولی:

```powershell
npm run electron:build:win
```

**زمان:**
- اولین بار: **5-10 دقیقه** (دانلود Electron برای Windows)
- دفعات بعد: **2-3 دقیقه**

**خروجی کامل:**
```
> electron:build:win
> electron-builder --win --x64 --target portable

  • electron-builder  version=24.x.x
  • loaded configuration  file=package.json ("build" field)
  • writing effective config  file=dist-electron\builder-effective-config.yaml
  • packaging       platform=win32 arch=x64 electron=28.x.x appOutDir=release\win-unpacked
  • downloading     url=https://github.com/electron/electron/releases/download/v28.x.x/electron-v28.x.x-win32-x64.zip size=95 MB parts=8
  • downloaded      url=https://github.com/electron/electron/releases/download/v28.x.x/electron-v28.x.x-win32-x64.zip duration=2m15s
  • building        target=portable arch=x64 file=release\سامانه مانیتورینگ POS-1.0.0-portable.exe
  • building block map  blockMapFile=release\سامانه مانیتورینگ POS-1.0.0-portable.exe.blockmap
```

**نشانه موفقیت:**
```
✓ built in 3m 25s
```

---

## 📂 مرحله 5: یافتن فایل Portable

### 5.1 بررسی پوشه release

```powershell
ls release
```

**خروجی مورد انتظار:**
```
Directory: C:\Users\...\release

Mode                 LastWriteTime         Length Name
----                 -------------         ------ ----
d-----        xx/xx/xxxx   x:xx PM                win-unpacked
-a----        xx/xx/xxxx   x:xx PM      xxxxxxxxx  سامانه مانیتورینگ POS-1.0.0-portable.exe
```

### 5.2 اطلاعات فایل Portable

```powershell
ls "release\سامانه مانیتورینگ POS-1.0.0-portable.exe"
```

**خروجی:**
```
Length: 250-350 MB (حجم تقریبی)
```

**این فایل است که می‌خواستید! ✅**

---

## ✅ مرحله 6: تست نسخه Portable

### 6.1 تست اولیه (در همین کامپیوتر)

1. **رفتن به پوشه release:**
   ```powershell
   cd release
   ```

2. **اجرای فایل:**
   - دوبار کلیک روی `سامانه مانیتورینگ POS-1.0.0-portable.exe`
   
   یا از PowerShell:
   ```powershell
   .\سامانه` مانیتورینگ` POS-1.0.0-portable.exe
   ```

3. **اولین اجرا:**
   - **Windows Defender** ممکن است warning بدهد
   - روی **"More info"** کلیک کنید
   - روی **"Run anyway"** کلیک کنید
   
   (این نرمال است برای برنامه‌های unsigned)

4. **چک کردن اجرا:**
   - ✅ پنجره برنامه باز می‌شود
   - ✅ Dashboard نمایش داده می‌شود
   - ✅ منوی سمت راست کار می‌کند (RTL)
   - ✅ هیچ پیغام خطایی نیست

### 6.2 تست پایگاه داده

1. **بررسی فایل دیتابیس:**
   ```powershell
   ls $env:APPDATA\pos-monitoring-system\
   ```
   
   **خروجی مورد انتظار:**
   ```
   pos-system.db
   ```

2. **تست عملکرد:**
   - در برنامه، به صفحه **شعبه‌ها** بروید
   - یک شعبه جدید اضافه کنید
   - برنامه را ببندید و دوباره باز کنید
   - ✅ اطلاعات باید ذخیره شده باشند

### 6.3 تست روی کامپیوتر دیگر (تست واقعی)

**این مهم‌ترین تست است! ⚠️**

1. **کپی کردن فایل:**
   - فایل `portable.exe` را روی یک USB کپی کنید
   - یا به کامپیوتر دیگری منتقل کنید

2. **کامپیوتر تست:**
   - **مهم:** کامپیوتری که **Node.js ندارد**
   - **مهم:** کامپیوتری که **این پروژه روی آن نیست**

3. **اجرا:**
   - فایل را دوبار کلیک کنید
   - **باید بدون هیچ خطایی اجرا شود ✅**

4. **اگر خطا گرفتید:**
   - Screenshot از خطا بگیرید
   - متن خطا را یادداشت کنید
   - به بخش [رفع مشکلات](#رفع-مشکلات) بروید

---

## 🔧 مرحله 7: رفع مشکلات

### مشکل 1: "npm is not recognized"

**علت:** Node.js نصب نشده یا PATH تنظیم نشده

**راه حل:**
```powershell
# 1. Restart کامپیوتر
# 2. PowerShell را دوباره باز کنید
# 3. تست کنید:
node --version
```

اگر باز هم خطا داد:
- Node.js را دوباره نصب کنید
- حتماً تیک "Add to PATH" را بزنید

---

### مشکل 2: "EACCES: permission denied"

**علت:** Antivirus یا Windows Defender مانع می‌شود

**راه حل:**
```powershell
# 1. Antivirus را موقتاً خاموش کنید
# 2. پوشه node_modules را پاک کنید:
rm -r node_modules
# 3. دوباره نصب کنید:
npm install
```

---

### مشکل 3: "gyp ERR! stack Error: spawn python ENOENT"

**علت:** Python برای build native modules نیاز است

**راه حل:**

این warning است، نه error! ادامه دهید:
```powershell
npm install --ignore-scripts
npm rebuild better-sqlite3
```

---

### مشکل 4: Build موفق شد ولی فایل portable نیست

**علت:** Target اشتباه انتخاب شده

**راه حل:**
```powershell
# دستور صحیح:
npm run electron:build:win -- --target portable

# نه این:
npm run electron:build:win
```

---

### مشکل 5: "Module not found: better-sqlite3"

**علت:** Native module rebuild نشده

**راه حل:**
```powershell
# Rebuild کردن:
npm rebuild
npm run electron:build:win -- --target portable
```

---

### مشکل 6: فایل exe روی Windows دیگر کار نمی‌کند

**علت:** Build در Replit/Linux بوده (همان مشکل اصلی!)

**راه حل:**
- ✅ **حتماً** روی ویندوز build کنید
- ❌ Build در Replit کار نمی‌کند

---

### مشکل 7: "Windows protected your PC"

**علت:** فایل unsigned است

**راه حل:**
1. روی **"More info"** کلیک کنید
2. روی **"Run anyway"** کلیک کنید

برای حذف این warning در آینده:
- Code signing certificate بخرید (~$200/سال)
- یا به کاربران آموزش دهید

---

## 📊 چک‌لیست نهایی

قبل از توزیع نسخه portable، این موارد را چک کنید:

### ✅ چک‌لیست Build:
- [ ] Node.js نصب شده (v20+)
- [ ] npm install اجرا شده
- [ ] npm rebuild اجرا شده
- [ ] npm run build موفق بوده
- [ ] electron:build:win با --target portable اجرا شده
- [ ] فایل .exe در پوشه release وجود دارد
- [ ] حجم فایل 200-400MB است

### ✅ چک‌لیست تست:
- [ ] فایل روی همین کامپیوتر اجرا می‌شود
- [ ] پایگاه داده ساخته می‌شود
- [ ] اطلاعات ذخیره می‌شوند
- [ ] فایل روی USB کپی شده
- [ ] روی کامپیوتر دیگر (بدون Node.js) تست شده ⭐
- [ ] همه قابلیت‌ها کار می‌کنند

---

## 🎉 تبریک!

اگر تمام مراحل را طی کردید، حالا یک فایل portable دارید که:

✅ روی **هر ویندوز 10/11** کار می‌کند
✅ **بدون نیاز به نصب** اجرا می‌شود
✅ **بدون Node.js** کار می‌کند
✅ مثل **VLC Portable** و **Firefox Portable** است

---

## 📞 پشتیبانی

اگر مشکلی داشتید:
1. Screenshot از خطا بگیرید
2. فایل `npm-debug.log` را چک کنید
3. با تیم پشتیبانی تماس بگیرید

**موفق باشید!** 🚀
